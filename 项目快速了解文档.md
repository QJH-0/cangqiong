# 项目快速了解文档

> 基于当前工作区 `D:\Code\workspace\cangqiong` 的实际文件结构整理。根目录没有独立 `README.md`，这是一个“后端主仓 + 小程序产物 + Nginx 管理端产物”的组合工作区，而不是单一应用仓库。

## 一、项目身份卡
- **项目名称**：苍穹外卖（后端 Maven `artifactId` 为 `sky-take-out`，接口文档标题也是“苍穹外卖项目接口文档”）
- **技术栈**：
  - 后端：Java 21、Spring Boot 2.7.3、Spring Web、Spring Cache、Spring Scheduling、Spring WebSocket、Spring AMQP
  - 数据层：MyBatis、Druid、MySQL、PageHelper
  - 中间件：Redis、RabbitMQ
  - 认证与能力：JWT、AOP、Knife4j、Logback MDC、阿里云 OSS、微信支付 API v3、百度地图、Apache POI
  - 前端交付形态：
    - `nginx-1.20.2\html\sky`：已构建的管理端静态站点
    - `mp-weixin\mp-weixin`：微信小程序编译产物/工程目录，带 `app.json`
- **项目类型**：餐饮外卖系统，属于“Web 管理端 + 小程序用户端 + Java 后端服务”的全链路业务项目
- **入口文件**：
  - 后端主入口：`sky-take-out\sky-server\src\main\java\com\sky\SkyApplication.java`
  - 小程序入口：`mp-weixin\mp-weixin\app.js` / `app.json`
  - 管理端静态入口：`nginx-1.20.2\html\sky\index.html`

## 二、一句话说清楚
这是一个“外卖后台管理 + 用户微信点餐下单”的系统，后端负责商品、员工、订单、优惠券、支付、权限和超时关单，小程序负责用户下单流程，Nginx 承载管理端页面并做接口转发。

## 三、核心功能
1. 管理端员工登录、员工管理、状态管理、基础权限控制
2. 菜品、分类、套餐、店铺营业状态管理
3. 用户端菜品浏览、购物车、地址簿、备注、历史订单
4. 订单提交流程、支付流程、订单详情、催单
5. WebSocket 推送来单/催单提醒
6. 订单超时自动取消，采用 RabbitMQ 死信队列
7. 优惠券活动创建与用户秒杀领券，采用 Redis Lua 保证原子扣减
8. 运营报表与 Excel 模板导出
9. OSS 文件上传、JWT 双端认证、全局异常处理、TraceId 日志链路

## 四、目录结构

```text
cangqiong/
├─ sky-take-out/                      # 主后端仓库，Maven 多模块
│  ├─ pom.xml                         # 父 POM，聚合 sky-common / sky-pojo / sky-server
│  ├─ docs/superpowers/               # 本轮改造计划、设计和 SQL 说明
│  ├─ sky-common/                     # 公共模块：常量、上下文、异常、JWT/OSS/微信工具等
│  │  └─ src/main/java/com/sky/
│  │     ├─ constant/                 # 常量
│  │     ├─ context/                  # BaseContext、AuthContext 线程上下文
│  │     ├─ exception/                # 业务异常
│  │     ├─ properties/               # 配置属性类
│  │     ├─ result/                   # 统一返回对象
│  │     └─ utils/                    # Jwt、OSS、微信支付、HTTP 工具
│  ├─ sky-pojo/                       # 数据模型模块
│  │  └─ src/main/java/com/sky/
│  │     ├─ entity/                   # 数据库实体
│  │     ├─ dto/                      # 请求 DTO
│  │     └─ vo/                       # 返回 VO
│  └─ sky-server/                     # 可运行服务模块
│     └─ src/
│        ├─ main/java/com/sky/
│        │  ├─ annotation/            # AutoFill、PermissionCheck
│        │  ├─ aspect/                # 自动填充、权限切面
│        │  ├─ config/                # MVC、Redis、RabbitMQ、WebSocket、过滤器配置
│        │  ├─ controller/admin/      # 管理端接口
│        │  ├─ controller/user/       # 用户端接口
│        │  ├─ filter/                # TraceIdFilter
│        │  ├─ interceptor/           # JWT 拦截器
│        │  ├─ listener/              # RabbitMQ 订单超时监听
│        │  ├─ mapper/                # MyBatis Mapper 接口
│        │  ├─ message/               # MQ 消息体
│        │  ├─ service/impl/          # 业务实现
│        │  ├─ task/                  # 定时任务补偿
│        │  └─ websocket/             # WebSocket 服务
│        ├─ main/resources/
│        │  ├─ application.yml        # 主配置
│        │  ├─ application-dev.yml    # dev 环境配置
│        │  ├─ mapper/                # MyBatis XML
│        │  ├─ lua/                   # Redis Lua 脚本
│        │  ├─ sql/                   # 数据库增量 SQL
│        │  ├─ template/              # 报表模板
│        │  └─ logback-spring.xml     # 日志配置
│        └─ test/java/com/sky/        # 单元测试
├─ mp-weixin/
│  ├─ project.config.json             # 微信开发者工具外层配置
│  └─ mp-weixin/                      # 实际小程序工程目录
│     ├─ app.js / app.json / app.wxss
│     ├─ pages/                       # 首页、订单、详情、支付、地址、我的等页面
│     ├─ components/                  # 小程序组件
│     ├─ common/                      # 编译后的 runtime/vendor/main
│     ├─ uni_modules/                 # uni-ui 等模块
│     └─ static/                      # 静态资源
├─ nginx-1.20.2/
│  ├─ conf/nginx.conf                 # 反向代理配置
│  └─ html/sky/                       # 管理端构建产物
└─ H1苍穹外卖.md                      # 学习/笔记文档
```

补充说明：
- 根目录没有 `package.json`、`pyproject.toml`、`go.mod`、`Cargo.toml`
- 当前工作区没有 `docker-compose.yml`、`Dockerfile`、`.env.example`
- 管理端和小程序端都更像“已交付产物”，不是完整前端源码仓

## 五、快速启动

### 1. 后端依赖
- JDK 21
- Maven 3.x
- MySQL
- Redis
- RabbitMQ

### 2. 配置位置
- 主配置：`sky-take-out\sky-server\src\main\resources\application.yml`
- 开发配置：`sky-take-out\sky-server\src\main\resources\application-dev.yml`

默认 dev 配置里依赖：
- MySQL：`localhost:3306/sky_take_out`
- Redis：`localhost:6379`
- RabbitMQ：`localhost:5672`
- 服务端口：`8080`

### 3. 初始化数据库
如果新环境缺少最近新增的权限、优惠券、日志相关表，先执行：

`sky-take-out\sky-server\src\main\resources\sql\2026-05-05-auth-order-coupon-logging.sql`

### 4. 启动后端

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
Set-Location D:\Code\workspace\cangqiong\sky-take-out
mvn -pl sky-server -am spring-boot:run
```

或直接在 IDE 运行 `com.sky.SkyApplication`。

### 5. 访问接口文档
- `http://localhost:8080/doc.html`

### 6. 运行测试

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
Set-Location D:\Code\workspace\cangqiong\sky-take-out
mvn -pl sky-server -am test
```

注意：
- `mvn -pl sky-server test` 单独跑会因为没有同时构建上游模块，出现 `AuthContext` 类缺失
- 正确命令需要加 `-am`

### 7. 启动 Nginx 管理端

```powershell
Set-Location D:\Code\workspace\cangqiong\nginx-1.20.2
.\nginx.exe
```

访问：
- `http://localhost/`

Nginx 转发规则：
- `/api/` -> `http://localhost:8080/admin/`
- `/user/` -> `http://localhost:8080/user/`
- `/ws/` -> `http://localhost:8080/ws/`

### 8. 打开微信小程序
- 用微信开发者工具导入 `D:\Code\workspace\cangqiong\mp-weixin\mp-weixin`
- 小程序页面路由定义在 `app.json`
- 当前仓库内是编译后工程，联调时优先确认请求地址和域名白名单

## 六、关键依赖说明

### 后端框架
- `spring-boot-starter-web`：REST API
- `spring-boot-starter-test`：测试
- `spring-boot-starter-cache`：缓存抽象
- `spring-boot-starter-data-redis`：Redis 访问
- `spring-boot-starter-websocket`：WebSocket 推送
- `spring-boot-starter-amqp`：RabbitMQ 消息能力

### 数据与 ORM
- `mysql-connector-java`：MySQL 驱动
- `mybatis-spring-boot-starter`：MyBatis 集成
- `druid-spring-boot-starter`：数据源连接池
- `pagehelper-spring-boot-starter`：分页

### 认证与文档
- `jjwt`：JWT 生成与解析
- `knife4j-spring-boot-starter`：接口文档页面
- `aspectjrt` / `aspectjweaver`：AOP 支撑

### 业务扩展
- `aliyun-sdk-oss`：上传文件到阿里云 OSS
- `wechatpay-apache-httpclient`：微信支付
- `poi` / `poi-ooxml`：Excel 报表导出
- `fastjson`：JSON 处理
- `lombok`：减少样板代码

### 小程序侧
- 不是 npm 工程
- 主要由微信小程序运行时 + uni-app 编译产物组成
- `common\vendor.js`、`common\runtime.js`、`common\main.js` 是打包输出

## 七、给 AI 模型的关键提示
1. 这是多模块 Maven 项目，改后端逻辑时要区分：
   - `sky-common`：公共能力
   - `sky-pojo`：实体/DTO/VO
   - `sky-server`：控制器、服务、配置、运行逻辑
2. 接口明显分成两套：
   - `com.sky.controller.admin`
   - `com.sky.controller.user`
3. 认证不是 Spring Security，而是“JWT 拦截器 + 线程上下文”模式：
   - `BaseContext` 存当前用户 ID
   - `AuthContext` 存当前身份
4. 权限控制不是统一网关，而是注解切面：
   - `@PermissionCheck`
   - `PermissionAspect`
5. 订单超时不是只靠定时任务，已经切到 RabbitMQ：
   - `OrderTimeoutPublisher`
   - `OrderTimeoutListener`
   - `RabbitMqConfiguration`
   - `OrderTask` 更像补偿
6. 优惠券秒杀依赖 Redis Lua，相关逻辑不要只改 Java：
   - `CouponServiceImpl`
   - `CouponSeckillScriptServiceImpl`
   - `resources\lua\coupon_seckill.lua`
7. 日志链路依赖 MDC：
   - `TraceIdFilter`
   - `logback-spring.xml`
8. 管理端和小程序端都不是标准源码仓：
   - Nginx 下是构建产物
   - 小程序目录里也是编译结果痕迹明显
   - 改前端时要先确认是不是在改产物，而不是改源代码
9. 根目录没有 README，很多项目约定只能从代码结构、POM、配置和设计文档反推

## 八、可能的问题点/复杂度
1. **工作区不是单仓结构**
   - 真正核心代码在 `sky-take-out`
   - `mp-weixin` 和 `nginx-1.20.2` 更像运行/交付物
   - 新人容易误判修改入口

2. **前端源码不完整**
   - 管理端只有静态构建产物
   - 小程序目录包含大量编译输出
   - 如果要做前端结构性修改，可能需要原始 Vue/uni-app 源码仓

3. **外部依赖较多**
   - MySQL、Redis、RabbitMQ、OSS、微信、百度地图都可能影响运行
   - 本地只起 Java 服务通常不够

4. **敏感配置直接出现在 `application-dev.yml`**
   - 包含数据库、微信、OSS、地图等配置
   - 建议尽快改为环境变量或私有配置覆盖

5. **订单链路复杂**
   - 下单、支付、WebSocket 提醒、MQ 超时取消、定时任务补偿同时存在
   - 修改订单状态机时要统一考虑

6. **权限模型是后加的**
   - 从 `docs\superpowers\specs` 和新增 SQL 可以看出这是近期增强点
   - 后续新增管理接口时，很容易漏加权限注解或初始化数据

7. **测试命令有坑**
   - 单跑 `sky-server` 会缺上游模块类
   - 需要 `mvn -pl sky-server -am test`

8. **编码/终端环境要注意**
   - 这个工作区里中文文件较多
   - PowerShell 输出未设 UTF-8 时容易出现乱码
