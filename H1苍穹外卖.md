# 苍穹外卖

springboot  mybits  jwt  mysql  redis  springtask alioss 

## Token

JWT（JSON Web Token）是一种基于 JSON 的轻量级身份验证令牌，其结构由三部分组成，通过 **`.`** 分隔，整体格式为：`header（签名算法）.payload.signature（签名）`

在微信开发中，使用后端返回的 token 主要涉及存储 token 以及在后续请求中携带 token 进行身份验证等操作，根据微信公众号、小程序等不同场景，使用方式略有差异，以下是具体介绍：

- 微信小程序
  - **存储 token**：后端将 token 返回给小程序后，小程序可使用`wx.setStorageSync('token', res.token)`将其存储起来，其中`res.token`是后端返回的 token 值。也可以使用异步存储方法`wx.setStorage()`，它接受一个对象参数，如`wx.setStorage({key: 'token', value: res.token})`。
  - **携带 token 请求**：当需要向后端发起业务请求时，从 storage 中取出 token，然后将其携带在请求头中。例如使用`wx.request()`方法发送请求时，可以这样设置：

```javascript
wx.getStorageSync('token').then((token) => {
    wx.request({
        url: 'your - server - url',
        method: 'GET',
        header: {
            'Authorization': `Bearer ${token}` // 这里的Bearer是常见的令牌前缀，可根据后端要求调整
        },
        success: (res) => {
            console.log(res.data);
        },
        fail: (err) => {
            console.error(err);
        }
    });
});
```



## Springboot

  三层架构 controller service mapper

Aop 自定义注解为数据库update insert操作添加注解 @Aspect注解

设置切入点以及注解条件

![image-20250512130950309](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250512130950309.png)

前置通知 等注解  ，通过***Joinpoint***获取切入点的方法

根据获取到的方法获取其参数（传入的实体）

通过反射调用实体的setter方法为公共字段填充数据

![image-20250512131643954](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250512131643954.png)

## SpringTask

定时处理任务，在启动类上添加@EnableScheduling注解开启注解，启用定时任务调用功能

@Scheduled（corn = " * * * * * *"）注解

秒 分 时 月 年 周？

0表示每分钟的第0秒处理数据 *=表示每分钟执行

![image-20250512132141911](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250512132141911.png)

## Spring MVC框架的拦截器

通过继承HandlerInterceptor接口

重写他的preHandler方法拦截Http参数获取其中的token校验jwt

在 Spring MVC 框架中，`HandlerMethod` 是一个核心概念，它代表**被 @RequestMapping 注解标记的控制器方法**

![image-20250512132639941](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250512132639941.png)

通过继承**WebMVCConfgurationSupport**类，重写他的**addIntercepor**方法注册拦截器的方法和具体拦截路径

![image-20250512134817256](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250512134817256.png)

## Spring MVC全局异常处理

@RestContorllerAdvice注解类，处理所有Controller抛出的Exception，并返回RestFul Json数据，统一响应格式

通过方法注解@ExceptionHandler和方法具体异常参数结合处理具体异常

![image-20250512133802668](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250512133802668.png)

## Spring MVC配置类

这类配置类通常继承自 `WebMvcConfigurationSupport` 或实现 `WebMvcConfigurer` 接口

1. **注册拦截器**：通过重写`addInterceptors`方法，将自定义的拦截器添加到 Spring MVC 的拦截器链中，实现对请求的预处理和后处理。比如在上述代码中，注册了`JwtTokenAdminInterceptor`和`JwtTokenUserInterceptor`拦截器，分别对`/admin/**`和`/user/**`路径下的请求进行拦截，对需要登录认证的接口进行权限验证。
2. **生成接口文档**：借助相关工具（如 Swagger、Knife4j 等）生成接口文档。在配置类中通过定义`Docket` bean 来配置接口文档的相关信息，如文档标题、版本、描述等，方便开发人员查看和使用接口。上述代码使用 Knife4j 生成了 “苍穹外卖项目接口文档”。
3. **配置静态资源映射**：通过重写`addResourceHandlers`方法，将特定的静态资源路径映射到项目中的实际资源目录，使得 Spring MVC 能够正确处理静态资源的请求。代码中将`/doc.html`映射到`classpath:/META - INF/resources/`目录下的`doc.html`文件，将`/webjars/**`映射到`classpath:/META - INF/resources/webjars/`目录，确保接口文档等静态资源能够被正确访问。
4. **扩展消息转换器**：重写`extendMessageConverters`方法，可以扩展或自定义 Spring MVC 的消息转换器。消息转换器用于在请求和响应过程中对数据进行转换，例如将 Java 对象转换为 JSON 格式的数据返回给客户端，或者将客户端发送的 JSON 数据转换为 Java 对象。上述代码中，通过扩展消息转换器，将自定义的`JacksonObjectMapper`设置到`MappingJackson2HttpMessageConverter`中，用于处理 JSON 数据的序列化和反序列化，可能是为了满足特定的日期格式转换等需求。
5. **配置视图解析器**：可以配置视图解析器，将处理方法返回的逻辑视图名解析为实际的视图对象，如 JSP、Thymeleaf 模板等，用于生成最终的响应页面。
6. **配置数据绑定**：可以自定义数据绑定规则，将请求参数绑定到方法参数上，支持自定义类型转换、数据格式化等功能。
7. **配置异常处理**：通过`@ControllerAdvice`和`@ExceptionHandler`注解结合，在配置类中定义全局异常处理方法，统一处理控制器中抛出的异常，返回友好的错误信息给客户端。

## Redis Sprigdata Springcache

**Spring Data Redis**是 Spring 框架中用于与 Redis 数据库进行交互的模块。它提供了一种简单且一致的方式来使用 Redis，将 Redis 的操作进行了封装和抽象，使得在 Spring 应用中使用 Redis 更加方便和高效

![image-20250528185737415](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250528185737415.png)

StringRedisTemplate的序列化

`StringRedisSerializer` 会将它们以 UTF - 8 编码的字节数组形式存储。从 Redis 中查看时，键和值都是可读的字符串形式。比如在 Redis 客户端中查看，键为 `"testKey"`，值为 `"testValue"` 。![image-20250528185901542](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250528185901542.png)

RedisTemplate的序列化

将 `User` 对象存储到 Redis 时，`JdkSerializationRedisSerializer` 会将其序列化为二进制字节数组。从 Redis 客户端中查看，键为 `"user"`，而值是一段不可读的二进制数据（因为是 Java 对象序列化后的结果）

![image-20250528190058943](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250528190058943.png)

```java
RedisTemplate.opsForValue.set(key, value)
```

通过在启动类上添加***@EnableCaching***注解，开启Spring的缓存功能

根据导入的Maven坐标决定使用哪一个缓存

```java
@Cacheable(value = "users", key = "#id")插入
@CachePut(value = "users", key = "#user.id")删除
@CacheEvict(value = "users", key = "#id")更新
```

![image-20250512143546200](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250512143546200.png)

## HttpClient

通过httpclient与第三方服务进行通信，例如微信支付接口。

![image-20250512224646295](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250512224646295.png)

## WebSocket

- 长连接，一旦连接数量过多会造成服务器性能下降
- 没有加密
- 对IE10以下的浏览器不支持

实现实时同信功能

![image-20250512224956987](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250512224956987.png)

是Java内部封装的Session接口，每个**Session**对象代表一个客户端和服务端连接实例

![image-20250512231000569](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250512231000569.png)

~~~js
WebSocket 通信需要 **客户端** 和 **服务端** 共同参与，两者的角色和创建方式不同：

1. **服务端**：需要主动创建 WebSocket 服务，监听特定端口，等待客户端连接。
2. **客户端**：需要主动发起连接请求，连接到服务端指定的地址和端口。

### 服务端创建 WebSocket 服务

服务端负责创建 WebSocket 服务器，监听连接请求。不同编程语言有不同的实现方式，以下是 Node.js 和 Python 的示例：

#### Node.js (使用 ws 库)

```javascript
const WebSocket = require('ws');

// 创建 WebSocket 服务器，监听 8080 端口
const wss = new WebSocket.Server({ port: 8080 });

// 监听客户端连接事件
wss.on('connection', function connection(ws) {
  console.log('客户端已连接');
  
  // 监听客户端消息
  ws.on('message', function incoming(message) {
    console.log('收到消息:', message);
    ws.send('服务器已收到: ' + message); // 回复客户端
  });
  
  // 监听连接关闭事件
  ws.on('close', function() {
    console.log('客户端已断开连接');
  });
});

console.log('WebSocket 服务器已启动，监听端口 8080');
```

#### Python (使用 websockets 库)

```python
import asyncio
import websockets

# 定义处理客户端连接的异步函数
async def echo(websocket):
    async for message in websocket:
        print(f"收到消息: {message}")
        await websocket.send(f"服务器已收到: {message}")

# 启动 WebSocket 服务器
async def main():
    async with websockets.serve(echo, "localhost", 8765):
        print("WebSocket 服务器已启动，监听端口 8765")
        await asyncio.Future()  # 保持服务器运行

asyncio.run(main())
```

### 客户端创建 WebSocket 连接

客户端需要在浏览器或其他环境中创建 WebSocket 实例，连接到服务端。以下是 JavaScript 的示例：

```javascript
// 创建 WebSocket 客户端，连接到服务端
const ws = new WebSocket('ws://localhost:8080');

// 监听连接建立事件
ws.onopen = function() {
  console.log('已连接到服务器');
  ws.send('你好，服务器！'); // 发送消息到服务器
};

// 监听消息接收事件
ws.onmessage = function(event) {
  console.log('收到服务器消息:', event.data);
};

// 监听连接关闭事件
ws.onclose = function() {
  console.log('与服务器的连接已关闭');
};

// 监听错误事件
ws.onerror = function(error) {
  console.error('WebSocket 错误:', error);
};
```

### 总结

- **服务端**：创建 WebSocket 服务，监听端口，等待连接。
- **客户端**：创建 WebSocket 实例，主动连接到服务端。

两者共同构成 WebSocket 通信的完整流程，实现双向实时通信。
~~~



## 回调函数



![image-20250513123819005](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250513123819005.png)

在封装的ajax请求方法中传入请求地址和回调函数

实际上，现在更推荐使用 Fetch API，它是 XMLHttpRequest 的现代替代方案，基于 Promise：

```java
// 简单的 GET 请求
fetch('https://api.example.com/data')
  .then(response => {
    if (!response.ok) {
      throw new Error(`HTTP 错误! 状态: ${response.status}`);
    }
    return response.json(); // 解析 JSON 响应
  })
  .then(data => {
    console.log('获取的数据:', data);
    // 更新 UI
  })
  .catch(error => {
    console.error('Fetch 错误:', error);
  });

// POST 请求示例
fetch('https://api.example.com/submit', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    name: 'John Doe',
    email: 'john@example.com',
  }),
})
  .then(response => response.json())
  .then(data => console.log('提交成功:', data))
  .catch(error => console.error('提交失败:', error));
```

## cookie session token

+ cookie : **服务器发送给客户端的信息**，保存在**本地**，每次请求时携带，大小限制4kB，。可设置**过期时间**
+ session：**存储在服务器**，通过**sessionID** 与客户端进行通信，一般保存在cookie中，session可存储任意大小数据。
+ token：客户端凭证，包含用户身份信息和权限，通过签名保证数据完整性
  + 如**Jwt**等，无状态，客户端和服务器都存在验证逻辑，跨域支持，可以通过http header传输，不依赖cookie
  + ![image-20250529220030113](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250529220030113.png)
  + ![image-20250528185132894](C:\Users\20448\AppData\Roaming\Typora\typora-user-images\image-20250528185132894.png)
  + **Cookie 是 HTTP Header 的一部分内容**：虽然 HTTP Header 包含了众多的字段和信息，但 Cookie 相关的字段（`Set-Cookie`和`Cookie`）是其中专门用于处理 Cookie 的部分



## 微信支付

![img](https://i-blog.csdnimg.cn/img_convert/4b11e86d3f35b01ba45177546f427e2f.png)
